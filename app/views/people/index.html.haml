.people-list
  %h1 People List
  %br
  %div
    %p.pull-left
      Here you will be able to find a list of the people that's currently stored in our database.
      Use the
      %code Add Person
      button to add more people.
    = link_to 'Add Person', new_person_path, class: 'btn btn-success pull-right'

  %br
  - if @people.present?
    %table.table.table-striped
      %thead
        %tr
          %th First name & Last name
          %th Email
          %th Age
          %th Delete Button
      %tbody
        - @people.each do |person|
          %tr
            %td= link_to "#{person.first_name} #{person.last_name}", person_path(person.id)
            %td= person.email
            %td= "#{pluralize(person.age, 'yr', 'yrs')}"
            %td= link_to 'Delete', person_path(person.id), class: 'btn btn-danger', method: :delete, data: { confirm: "Are you sure you want to delete #{person.first_name} #{person.last_name}?" }
  - else
    %br
    %br
    .well
      %h4.text-center There aren't records created yet